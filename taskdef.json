{
  "family": "flask-docker",
  "containerDefinitions": [{
    "image": "%REPOSITORY_URI%:v%BUILD_NUMBER%",
    "name": "flask-docker",
    "cpu": 10,
    "memory": 256,
    "essential": true,
    "portMappings": [{
      "containerPort": 5000,
      "hostPort": 5000
    }]
  }]
}